import { Wrapper, NavContainer, Logo } from './styles';

const Header = () => {
	return (
		<Wrapper>
			<NavContainer>
				<Logo>- QrGen</Logo>
			</NavContainer>
		</Wrapper>
	);
};

export default Header;
